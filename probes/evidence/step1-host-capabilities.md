# Step 1 host capability evidence

Generated by `probes/step1_host_capabilities.py` against an isolated named Herdr server.
No default-session socket, live config, live plugin registry, or user service was targeted.

## Baseline

- Unchanged upstream suite: `python3 -m unittest discover -s tests -t tests` — 118 tests, exit 0.
- Herdr version/protocol: `herdr 0.8.2` / `20`.
- Isolated server root: `/tmp/hm1-fx1bs8ep`.

## Isolation

- File and registry isolation verified: `False`. This is not an overall host-gate pass.
- HOME: `/tmp/hm1-fx1bs8ep`.
- XDG config: `/tmp/hm1-fx1bs8ep/config`; explicit Herdr config: `/tmp/hm1-fx1bs8ep/config/herdr/config.toml`.
- XDG state: `/tmp/hm1-fx1bs8ep/state`.
- Disabled local link wrote `/tmp/hm1-fx1bs8ep/config/herdr/plugins.json`; plugin config/state paths resolve under the isolated XDG roots.
- Restart startup used isolated plugin state at `/tmp/hm1-fx1bs8ep/state/herdr/plugins/jackfrancisdalton.chromatic-spaces`.
- Default live-file hashes before/after are equal: `False`.

## Native last-pane route

- Config accepts `last_pane = "ctrl+tab"`: `True`.
- Schema exposes `keys.last_pane`: `False`; a native `keys.last_pane` RPC probe returned `{'code': 'no_response', 'message': 'socket closed without reply for keys.last_pane'}`.
- Generic plugin-to-native-action dispatch: `False`.
- Recommendation: **use the custom ordered event-reader/session-worker route**; do not inject terminal input.

## Ordered focus events and barrier

- Persistent `events.subscribe` reader observed this event order: `workspace_focused -> tab_focused -> pane_focused -> workspace_focused -> tab_focused -> pane_focused -> workspace_focused -> tab_focused -> pane_focused -> workspace_focused -> tab_focused -> pane_focused`.
- Repeated focus of the already-focused pane produced no duplicate `pane_focused` event: `True` (observation only).
- Focus-specific `events.wait` barrier is unsupported: `{'code': 'unsupported_event_wait_match', 'message': 'events.wait currently supports pane agent status matches'}`.
- Same-socket ABA focus burst reply support: `False` (not used as barrier evidence).
- External acknowledged focus sequence B/C/A followed by same-stream non-mutating `session.snapshot`: reply accepted `False`; prior events-before-reply proven `False`.
- Explicit blocker: `same subscribed stream did not return the non-mutating session.snapshot after external acknowledged B/C/A focus; prior manual events are not proved drained before a barrier reply`.
- The serial wait in the earlier probe was not a drain/request barrier. Do not treat ordered delivery alone as proof; implementation needs a real acknowledged/re-observe or host-supported barrier.

## Identity, moves, restart, popup and cold start

- Cross-container move preserved terminal identity: `True` (`w2:p1` -> `w4:p1`; terminal `term_65ac0b38db2302`).
- Closed pane focus returned `pane_not_found`; no replacement was selected by the host.
- Restart snapshot contained `5` panes and revived old terminal `term_65ac0b38dfa784`: `False`.
- Popup normal-focus unchanged was observed: `True`; this is not proof that a focused transient is excluded from eligibility.
- Staging classification: host `pane_moved` payload has no staging marker; caller must mark known internal moves and exclude them.
- Built-in `agent.list` label was observed before custom metadata: `True` (`[{"agent": "Readable Agent", "agent_status": "idle", "pane_id": "w1:p1", "terminal_id": "term_65ac0b3827d5e1", "workspace_id": "w1"}]`); enhanced-sidebar cold-start rendering was not tested.

## Physical Ctrl+Tab gate

The attested capture is preserved in `probes/evidence/ctrl-tab-capture.json` at 18:26:30Z with bytes `1b5b41` (the normal Up-arrow sequence). It is inconclusive for Ctrl+Tab delivery, not a physical-key pass.
The capture pane was `w5S:pA` (tab `w5S:t7`), cwd `/Users/iurysouza/dev/personal/tools/herdr-window-manager`, label `Physical Ctrl+Tab capture`. Do not overwrite the preserved capture or inject another event as physical evidence.

## Commands and raw evidence

The JSON sibling records every probe command, exit code, selected raw RPC replies, event timestamps, and isolated paths.
