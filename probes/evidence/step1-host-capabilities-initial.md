# Step 1 host capability evidence

Generated by `probes/step1_host_capabilities.py` against an isolated named Herdr server.
No default-session socket, live config, live plugin registry, or user service was targeted.

## Baseline

- Unchanged upstream suite: `python3 -m unittest discover -s tests -t tests` — 118 tests, exit 0.
- Herdr version/protocol: `herdr 0.8.2` / `20`.
- Isolated server root: `/tmp/hm1-yd0z6eah`.

## Isolation

- Result: **PASS**.
- HOME: `/tmp/hm1-yd0z6eah`.
- XDG config: `/tmp/hm1-yd0z6eah/config`; explicit Herdr config: `/tmp/hm1-yd0z6eah/config/herdr/config.toml`.
- XDG state: `/tmp/hm1-yd0z6eah/state`.
- Disabled local link wrote `/tmp/hm1-yd0z6eah/config/herdr/plugins.json`; plugin config/state paths resolve under the isolated XDG roots.
- Restart startup used isolated plugin state at `/tmp/hm1-yd0z6eah/state/herdr/plugins/jackfrancisdalton.chromatic-spaces`.
- Default live-file hashes before/after are equal: `True`.

## Native last-pane route

- Config accepts `last_pane = "ctrl+tab"`: `True`.
- Schema exposes `keys.last_pane`: `False`; a native `keys.last_pane` RPC probe returned `{'code': 'no_response', 'message': 'socket closed without reply for keys.last_pane'}`.
- Generic plugin-to-native-action dispatch: `False`.
- Recommendation: **use the custom ordered event-reader/session-worker route**; do not inject terminal input.

## Ordered focus events and barrier

- Persistent `events.subscribe` reader: `True`; observed event order: `workspace_focused -> tab_focused -> pane_focused -> workspace_focused -> tab_focused -> pane_focused -> workspace_focused -> tab_focused -> pane_focused -> workspace_focused -> tab_focused -> pane_focused`.
- Repeated focus of the already-focused pane produced no duplicate `pane_focused` event: `True`.
- Focus-specific `events.wait` barrier: `{'code': 'unsupported_event_wait_match', 'message': 'events.wait currently supports pane agent status matches'}`.
- The stream is ordered and targetable, but has no focus sequence field; the worker must own one reader, drain pending events, and treat a lost focus reply as uncertain.

## Identity, moves, restart, popup and cold start

- Cross-container move preserved terminal identity: `True` (`w2:p1` -> `w4:p1`; terminal `term_65ac08034007d2`).
- Closed pane focus returned `pane_not_found`; no replacement was selected by the host.
- Restart snapshot contained `5` panes and revived old terminal `term_65ac0802b681d1`: `False`.
- Popup opened without changing the normal focused pane: `True`; popup is not represented as a normal snapshot pane in headless mode.
- Staging classification: host `pane_moved` payload has no staging marker; caller must mark known internal moves and exclude them.
- Built-in cold-start agent name was readable before custom metadata: `True` (`[{"agent": "Readable Agent", "agent_status": "idle", "pane_id": "w1:p1", "terminal_id": "term_65ac08028e2a31", "workspace_id": "w1"}]`).

## Physical Ctrl+Tab gate

A separate non-injecting capture is armed in `w5S:pA` (tab `w5S:t7`), cwd `/Users/iurysouza/dev/personal/tools/herdr-window-manager`, label `Physical Ctrl+Tab capture`.
The operator must focus/click that pane in Ghostty and press Ctrl+Tab once physically. Do not use `herdr pane send-keys`, `agent send-keys`, or `termctrl`; require operator attestation. A capture before attestation is not a pass or failure.

## Commands and raw evidence

The JSON sibling records every probe command, exit code, selected raw RPC replies, event timestamps, and isolated paths.
