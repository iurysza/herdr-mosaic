# Execution evidence

Bounded captures for the TypeScript port live here. Each passing inventory
row should cite a file in this directory or a test name plus a revision.

Do not store credentials, live user config, or unrelated home-directory data.
